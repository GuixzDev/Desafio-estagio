Desafio Fullstack

Formulário (Angular): http://localhost:4200
Dashboard (React): http://localhost:3000
API: http://localhost:8000
Swagger: http://localhost:8000/docs

`POST /records`: cdrasta um novo registro
`GET /records`: Lista os registros ordenados por data decrescente
`GET /summary`: Retorna dados consolidados
`GET /employees`: Lista os funcionários cadastrados